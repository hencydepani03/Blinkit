# Vite env file template for frontend
# Copy this to .env.local for local development
# For production deployment, set values in your platform or create .env.production

# Backend API base URL
VITE_API_URL=https://your-backend-domain.com

# App metadata
VITE_APP_NAME=Blink Shop Now
